# React Cheat Sheet

npm create vite@latest

1. Variable

const name = "Lokwx";

<h1>{name}</h1>

{} = enter JavaScript mode


2. Component

function MyButton() {
    return (
        <button>Hello</button>
    );
}

<MyButton />


3. Props (send data)

<MyButton text="Add" />

function MyButton({text}) {
    return (
        <button>{text}</button>
    );
}


4. State (remember values)

const [count, setCount] = useState(0);

setCount(count + 1);


5. Button click

function handleClick() {
    alert("Clicked");
}

<button onClick={handleClick}>
    Click
</button>


6. List

const foods = ["Laksa","Burger"];

{foods.map(food => (
    <h2>{food}</h2>
))}